from functools import reduce


numbers = [3, 2, 8, 3, 11]

print(list(map(lambda n: n**2, numbers)))

# square of every number from 1 to 1 Million

# Generator objects with list comprehensions

# reduce doesn't have a counter part, but for numbers you can use math functions to reduce like sum
