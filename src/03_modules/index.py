from ctypes import util
from utils import add, multiply, VARIABLE

# when importing a module, the file.py that represents the module is executed. This means that if the module contains side effects like "print" they will be executed as well
